services:
  agefrontend:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_AGENT_API_URL=http://localhost:8000
    command: npm run dev -- --host 0.0.0.0
